# 🚀 Инструкция по загрузке проекта на GitHub

## Шаг 1: Установка Git

Git не установлен на вашем компьютере. Установите его одним из способов:

### Вариант 1: Официальный установщик (рекомендуется)
1. Скачайте Git: https://git-scm.com/download/win
2. Запустите установщик
3. Следуйте инструкциям (можно оставить настройки по умолчанию)
4. **Важно**: Перезапустите PowerShell после установки

### Вариант 2: Через winget
```powershell
winget install --id Git.Git -e --source winget
```

### Вариант 3: Через Chocolatey
```powershell
choco install git
```

## Шаг 2: Проверка установки

После установки и перезапуска PowerShell выполните:
```powershell
git --version
```

Должна отобразиться версия Git (например, `git version 2.42.0`)

## Шаг 3: Настройка Git (первый раз)

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

## Шаг 4: Загрузка проекта на GitHub

### Автоматическая загрузка (рекомендуется)

Просто запустите скрипт:
```powershell
.\scripts\upload-to-github.ps1
```

Скрипт автоматически:
- ✅ Проверит наличие Git
- ✅ Добавит все файлы
- ✅ Создаст коммит
- ✅ Отправит на GitHub

### Ручная загрузка

Если хотите сделать вручную:

```powershell
# 1. Проверка статуса
git status

# 2. Добавление всех файлов
git add .

# 3. Создание коммита
git commit -m "Initial commit: Arizona RP Gear Configurator"

# 4. Отправка на GitHub
git push origin main
```

## Шаг 5: Настройка аутентификации GitHub

При первом `git push` GitHub запросит аутентификацию.

### Создание Personal Access Token

1. Перейдите на: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Назовите токен (например, "Arizona Configurator")
4. Выберите срок действия (рекомендуется: 90 дней или без срока)
5. **Выберите права**: отметьте `repo` (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **Скопируйте токен** (он показывается только один раз!)

### Использование токена

При выполнении `git push`:
- **Username**: ваш GitHub username
- **Password**: вставьте скопированный токен (НЕ ваш пароль GitHub!)

Windows сохранит токен в Credential Manager, и в следующий раз он не потребуется.

## ✅ Проверка

После успешной загрузки откройте:
https://github.com/A-configurator/arizona-rp-gear-configurator

Все файлы должны быть там!

## 🔄 Дальнейшая работа

После настройки вы можете использовать скрипт для быстрых обновлений:

```powershell
.\scripts\commit-and-push.ps1 "Описание изменений"
```

Или работать с Git вручную - все уже настроено!

## ❓ Проблемы?

### Ошибка "git: command not found"
- Убедитесь, что Git установлен
- Перезапустите PowerShell
- Проверьте, что Git добавлен в PATH

### Ошибка аутентификации
- Проверьте, что токен создан с правами `repo`
- Убедитесь, что используете токен, а не пароль
- Попробуйте удалить сохраненные учетные данные:
  ```powershell
  git credential-manager-core erase
  ```

### Ошибка "remote origin already exists"
- Это нормально, remote уже настроен
- Просто продолжайте с `git push origin main`
